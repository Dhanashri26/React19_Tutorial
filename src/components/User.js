import React from 'react';

//Component is nothing but a function.
//Purpose of the function--> It will return some html code. We can use this function in our application to render the html code.
const User = () => {
    return (
        <div>
           <h1>This is a User Component</h1> 
        </div>
    );
};

export default User;
